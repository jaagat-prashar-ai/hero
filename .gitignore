# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/

# Bazel (convenience symlinks + caches)
bazel-*

# uv
.python-version

# Local bazel overrides
user.bazelrc

# Experiment tracking / data (created at runtime)
wandb/
/tmp/mnist_data/

# Alpamayo venv (installed packages, not source)
perplexity/alpamayo/ar1_venv/

# Cached video clips (large binary data, regenerated at runtime)
perplexity/clip_cache/

# IDE
.idea/
.vscode/
*.swp
.claude/settings.local.json

# Local working notes (not for the remote)
code_as_a_reward/conversation_log.md
