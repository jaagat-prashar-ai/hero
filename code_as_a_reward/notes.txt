# The goal for this project is to score Alpamayo reasonign quality by checking chain-of-causation (CoC) claims against structured scenes tate, building on the previous work with the pref-fairs faithfulness work. 

# Phase 0 (thsi is the feasibility investigation).
# The HF dataset has obstacle.offlien actor tracks (real 3D bounding-box tracks, present in 97.4% of clips), but no lane geometry or traffic-light ground truth anywhere
# A 120-trace claim taxonomoy showed that ~62% of the claims would be actor-checkable once we integrate obstacle.offline is integrated, while ~27% of it comes from (cones, traffic signals) have no ground truth at all. 

# Implementation - CoC claim parser 
# This is a rule-based parser tuning CoC text into typed claims such as the CommitmentClaim, PerceptualClaim, and CausalClaim - built up incrementally (typed dataclasses -> lexicons -> beat/clause segmentation -> commitment and perceptual extraction -> orchestration with unparsed-span tracking)
# Lexicons (maneuevers, entities, states, connectives) were tuned against the real corpus in pre_pairs/results/scene_reasoning/*.md, and the CLI ingests those .md reports directly. 
# 12 unit tests also in coc_claim_parser_test.py (written against the real corpus strings)

# To-do: claim verifier itself (checking commitment claims against TrajectoryFeatures are perceptual claims against obstacle.offline)
# The obstacle.offline integration to feed that verifier
# Feasibility and design decisions are settled, the parsing half of the pipeline is built and tested, and the verifictation half is next. 

# The whole idea of code as a reward is to score the reasonign automatically, check whatever the model said actually matches reality (i.e., the recorded scene data and the trajectory it drove)
# We can't check a free-form paragraph agianst structured data directly (the parser is the bridge we implement)
# The parser reads the CoC text and converts it into small, typed, machine-checkable statements ("claims"). Once the reasoning is in that structured form, a downstream verifier (still building this) will test each claim against the ground truth and turn the result into a reward signal. 

# The three claim types, using that example sentence: 

# PerceptualClaim: "i see x." an assertion that some entity exists in the scene, optinally with a state: there is a pedestrian, and it is crossing. This is checkable against the obstacle.offlien actor tracks in the datset - was there really a pedestrian there?
# CommitmentClaim: "i will do y." A maneuever or speed profile the model says it's going to execute: slow down, keep straight. This is checkable against the trajectory the model actually produced (its TrajectoryFeature) - did it really slow down?
# CausalClaim: - the "because" glue linking th etwo: I'm slowing down because of th epedestrian. It ties one or more commitments (the effects) to the preceptions that supposedlt causex them. This is what lets you catch reasoning that's unfaithful even when its parts are individually true (e.g., the pedestrian exists and the4 card di dslow, but the stated cause-effect link doesn't hold up)

# Perceptiual is what the model claims tos ee, comitment is what it does, causal is why it claims one led to the other (parser exists so each of those can be fact-checked by code instead of by a human or another LLM).

# how does the parser work?

# rule-based pipeline (regexes and lexcions tuned against ~2000 real CoC strings from pref_pairs/results_scene_reasoning/*md).

# Example sentence: "Nudge left due to construction cones blocking th eright side of our lane, then accelerate"

# Step 1: Split this into beats. The text is split on :, then, or sentence-ending periods (_split_beats). Each beat is one plan step. 
Here the beat 1 is "Nudge left due to construction cones blocking...," beat 2 is "accelerate." 

# Step 2: split each beat on its causal connective. Within a beat, _split_beat looks for "because / due to / since / as" (with "after / for" as a waker fallback tier, only used if no string connective exist). Everything before the connective is the commitment clause (what ego will do), everything after is the cause clause (why). 
A beat with no connective is fine (it's just a commitment with no reason).
# Thought: does this also work for the prefix idea? look more into this!!!!!!!!!!

# Step 3: extract commitments from the commitemnt clause only. MANEUVER_PATTERSN (~17 regexes: nudge, lane change, merge, accelerate, stop, yield, ...) are scanned over the commitment side. Each match becomes a CommitmentClaim with a canonical manauever key, an axis (lateral vs longitudinal), and optionally a direction: "left/right" is searched in a 40-char windo after the verb, capped at the next maneuever verb so compound commitments like "change lanes to the right and enter the ramp" don't both grab the ame "right." Scopin to the commitment side is delibearte: it prevents "vehicle merging from the right" in a cause clause from being read as ego's own merge. 

# Step 4: Extract perceptual claims from the whole beat. ENTITY_PATTERNS (~25 regexes: cones, pedestrian, stopped_vehicle, signal, work_zone, ...) find entity mentions, then each entity is paried with the nearest matching state_word from STATE_PATTERNS (blocking, crossing, red, green, ...). It scans the whole beat reather than just the cause clause because entity and state sometimes straddle the connective ("Adapt speed for the roundabout since a yield-controlled enty is ahead"). 

# Step 5: Assemble causal claims. If a connective was found, the beat becomes one CausalClaim: effects = the beat's commitments, cause = the subest of its perceptual claims whose span sits after teh connective. So here nudge(left) <- "due to" <- construction_cones (blocking).

# Step 6: bookeeping of what was missed (add thi slater)

# writing notes again because it helps tos cope stuff out: 
# Thinking about the goal a bit more: The goal is to score Alpmayo reasoning quality by checking chain-of-causation (CoC) claims against the structured scene state, building on the previous work from pref-pairs faithfulness work. 

# Phse 0: groundwork: 
# The upstream HF dataset has obstacle.offline actor racks (real 3D bbox tracks, present in 97.4% of the clips), but there is no lane geometry or traffic-light ground truth anywhere. 
# Model decision: we will use Alpamayo 1.5 (RL post-trained, with an existing harvester), rather than AR1-10B
# Built a 120-trace claim taxonomy: ~62% of claims are actor-checkable once obstacle.offline is integrated; ~27% (cones, signals) have no ground truth at all. Why?

# Implementaiton:
# coc_claim_parser.py: a rule-based parser that turns CoC text into typed claims - CommitmentClaim, PerceptualClaim, CausalClaim - with unparsed-span tracking so you know what the praser couldn't account for. 
# built up in layers: claim dataclasses -> maneuever/entity/state/connective lexicons (tuned against the real corpus in pref_pairs/results/scene_reasoning/*.md) -> beat/clause segmentation -> commitment and perceptual claim extraction -> parse_coc_trace orchestration. 
# A CLI that ingests those scene_reasoning .md reports, and 12 unit tests written against real corpus strings (coc_claim_parser_test.py)

# Not yet done: 
# The claim verifier itself (i.e., checking commitments against the TrajectoryFeature and perceptual claims against obstcle.offline)
# The obstacle.offline integration that verification depends on. 
# An open question here: Whether beat/connective splitting also works for prefix-style CoC text (i.e., "does this also work for the prefix idea?")

# Understand if this approach is the best way and how we can do better?

# We have worked on parsing (the parsing half is done). Next steps (concretely): 
# obstacle.offline integration (the blocker for everything else): 
    # The download path already exists (i.e., third/party/alpamayo-recipes/scripts/download_pai.py)
    # This already passes obstacle.offline in its --labels flag. What's missing is a loader that turns the raw label files (what specific files are tehse, double check) into a queryable state: per clip, a list of actor tracks with class (pedestrian, vehicle, etc.), 3D positions over time, the ego-relative geometry (distance, side of ego, ahead/behind). This is the ground-truth side that perceptual verification reads from. 

# The commitment verifier - CommitmentClaim vs TrajectoryFeatures  
    # This is the easier half? pref_pairs/trajectory_features.py:113 was explicitly designed for it (this is everything that the maneuever classiifer and the downstream claim verifier need). The work here is mapping from each canonical maneuever key the parser emits (nudge, stop, yield, accelerate, lane change, ...) to a predicate over the feature row -- e.g., stop -> stop_event, yield -> yield_event, nudge(left) -> sign/magnitude of final_lateral_offset_m, accelerate -> mean_acceleration_mps2 above a threshold. 
    # Thresholds can start from the same config values the manuever classifier uses. Since this half has zero dependencies, I'd build it first while deciding how to structure the obstacle loader. 


# basic run good
# next run: 
# dataloader_shuffle = true (the fixed clip ordering makes metrics hard to read and correlates gradient batches)
# more clips (num_reasoning_clips from 16 to 64+), 15 epochs over 16 clips is heavy reptition; memorization risk with any higher lr 
# the hard-clip group (L2 ~7.7) fails the ADE gate unconditonally - the graded band keeps it contributing gradient now, but if those are 00D clips you care about, consider whether ade_threshold=3.0 is right for them. 
# One unexplained 733s stall remains (between steps ~mid-run) - survivable under the 1h timeout, bit if it recurs at scale it's worth pulling the per-process logs while the node is alive

# Main changes compared to previous training run: alpamayo-rl-llm-judge-canary-grq1cf
# Change 3: enable dataloader shuffle (fixes the period-3 metric artifact from the canary)
# Change 4: the full-run cluster config + launch command

# 392 OOD clips across the 100 densest chunks (the empty-events filter trims 9 of the 1740). Launching: 
# Selector verified 392 OOD clips across the 100 densest chunks (empty-events filter trims 9 of the 1740). Launching: 

# What it trains on: 392 OOD clips (every OOD clip inside the 100 chunk densest in OOD clips - x 3 epochs = ~290 GRPO steps with shuffled dataloading).
# That's 24x canary's clip count for ~570 GB of download instead of the 6.1 TB that literally-all-1740 would have cost (they're scattered 1.6-per-chunk across 1085 chunks).

# S3 point: the recipe can't consume the WDS mirror directly (format mismatch + its camera content failed validation), but the job now mirrors the raw-PAI dataset to 
# s3://research-datasets-chicago/pai_warm_cache/ood_dense_100chunks in the background after the HF download - so any preemption/requeue restores it in-region in minutes instead of re-paying the 1-2 HF pull 

# Commits: OOD-dense chunk selector + reasoning_dense_chunks mode (verified locally: 392 clips/100 chunks)
# + part swept into your "notes" commit, S3 warm cache restore/upload#
# dataloader_shuffle = true (kills the period-3 metric artifact)
# llm_judge_full_cluster.yaml + launch.sh llm-judge-full

# Timeline: ~1-2 hour dataset download (first time - future runs hit the warm cache), then ~2.5-3h training with ~14k judge calls. WB experiment: 
# reasoning_vla_llm_judge_full in research/alpamayo-rl. Remember the OCI log stream stays quiet during training - W&B is the live signal. 

# the checkpoints still write to node-local /mnt/work with resume=false, so a preemption restarts training from step 0 (dataset restore is cheap now, but the training progress isn't checkpointed off-node). If preemptions bite, S3-backed checkpointing is the next piece worth building?
# Saved details to run memory so any later session can pick up monitoring.