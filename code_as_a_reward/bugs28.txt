bug fixes

What shipped:

- e901539 — the fix, two files:
  - run.py: new _extract_obstacles_by_clip() unzips every per-clip obstacle parquet out of the chunk zips into obstacles_by_clip/ during the CPU-only setup phase. Idempotent, runs only in the code reward branch, before GPUs are reserved.
  - code_reward_entry.py: _load_scene now reads obstacles_by_clip/<clip_id>.obstacle.offline.parquet directly (a ~250 KB file open), falling back to the old recipe-interface path if the extraction is missing. Docstring updated to match.
- 3cc4a7c — BUGS.md entry documenting the stall, the falsified parse theory, the judge-run comparison that localized it, and the fix.

Verified: all 63 code_as_a_reward tests pass, plus a smoke test that loaded the testdata clip through the new fast path (41 tracks, no alp_state needed).

Next step when you're ready: relaunch the code-reward run. Success signal is step time — if the fix is right, steps should run at roughly the judge-run cadence (~1 min) instead of 2/hour, and a 45-step run should finish in about an hour instead of 17. If ~51-minute stalls still show up, scene loading is definitively exonerated and we look at the rollout/infra side instead.

# Terminals: 

# gating verification (is our verifier good in that the wrong traces don't pass and the other ones do)?
# RL LLM-judge reward scene-grounded: scene_overlay.py, scene_reference_dataset.py, llm_judge.py, aggregated_reward_llm_judge.py, llm_judge_entry.py
# commitment and perceptual verification on a few data logs:  
  # Headline results. Machinery works: parsing, both verifiers, causal conjunction, and reward aggregation all run end-to-end, and hallucination-catching works (the fake scooter vs caught; real pedestrians verified)

# Bugs 29 

 #Canary eivn91 stack samples caught the rolloout parked in post_rollout_completion -> make_request_with_retry -> time.sleep(jitter)
 # (client.py:570, network_util.py:135) while al 4 policy ranks spun in grouped_send -> nccl_group_send -> time.sleep(0.001) (rl_worker.py:410, pynccl.py:228) awaiting the weight-snc collective. 
# One failed localhost HTTP POST idles all 5 GPUs. 

# The ~51 min quantum is cosmos-rl's stock retry ladder exhausting, not any computation: max_retries=60, jitter (1+random())*delay, initial_delay=1s, backoff x2 capped at 60s, jitter (1+random())*delay => expected 3172 seconds = 52.9 minutes. 
# a1npli's 14 slow steps measured 50.0-54.2 min in 1x/2x/3x multiples (19 quanta); train/iteration_time stayed flat at 22s because the stall is entirely outside the training step. 

# train/iteration_time stayed flat at 22s because the stall is entriely outside the training step 

# Capping max_retries to 20 (~1.9 min) alsio makes client.py:578's except run, logging the underlying exception at ERRO -- the remaining unknown, since per-attempt failures log at DEBUG. 

# Also raises _CosmosLogTrailer's 80-line cap to 1200: eivn91 dropped 4900 lines in 25 min and, because the cap keeps the tail, always discarded the stack-sample header. 
# Supersedes the obstacle-parse attribution in run.py: 885-889 and e901539: _load_scene measures 0.10-0.55s per cold clip in production (1.96s for all 12), and code_scene_available was 1.00 on all 35 of a1npli's steps. 

# so lower the max retries? and 

# bug update 
# validated: 16.7 h -> 62.5 min 

# Steps: 
# Before: 35 vs 32
# Wall clock: 16.7 hours to 62.5 minutes
# Steps > 20 min (14 vs 0)
# Median step (0.5 min vs 0.57 min)
# Worst step (158 min and 6.1 min)
# iteration_time (22 s vs 22 s)

# ~16x faster and code_atomic_precision / code_reward_raw not carry real values on every step? why?

# The complete root cause: 
  # code_reward_entry.py:366-369: 
    # "code_reward_raw": float(r) if r is not None else math.nan, 
    # "code_atomic_precision": float(atomic_precision) if atomic_precision is not None else math.nan, 

    # How do we decide the decided claims? Look into this! 
    # In which cases do we observe this? 
    # When a rollout has zero decided claims, r = (n_pass / decided) if decided else None -> None -> the code writes math.nan as a "no data" sentinel. 

# Reward pipeline code; starting from where the NaN sentinel is written, then the decided/abstain logic upstream - and build a lit of diagnostics to run. 

# Bugs July 30th: 
# Apply the metrics: 
  # Fixes the latent NaN-poisoning and the missing-key 0-bias that already fired once in vtx3ys; W&B gains code_undecided_cnt / code_no_cot_cnt visibility. 
  
  # Abstain reward: 
  # How should the reward treat an all-abstain (zero-decided) trace, given it currently gets neutral ~0.2 and PASSES the gate while a half-verified trace (~0.5) FAILS it?
  # Coverage-scale credit: 
     # Blend: effective r = decided_fraction * r + (1 - decided_fraction) * 0.8 neutral prior. Smooth, no cliff at zero-decided, removes the "say only unverifiable things' incentive; undecided traces keep exactly today's -0.2. Also apply unparsed_penalty in the undecided path. 
     
     # How do we quantify pass, fail, and abstain?

# 110 tests pass and the fill logic checks out? 

# How do we decide the decided claims? 
  # Commitment claims are decided against the rollout's own trajecotry kinematics (except relative-to-agent ones like keep_distance, which abstain)? why?
  # Perceptual claims are decided against obstacle.offline tracks (abstaining for no-ground-truth classes like cone/crosswalks/lanes, and for unverifiable attributes like "lead vehicle"); causal claims are a conjunction that abstains if either side does (i.e., of comitment or perceprtual is abstain, causal becomes abstain as well).
    # TO-DO: we currently abstain for no-ground-truth classes, but how do we include them? Can we make a dict for cones/crosswalks/lanes, etc. for the protruding objects? How do we be more inclusive here? Run object detection on the model with depth estimation ro something like gassuain splatting?

    # Zero-decided claims - what is this failure case? A trace made only of abstaining claims. The canonical shape is "keep a safe distance from the lead vehicle."
    # The single most natural driving prhasing (where both the commitment and perceptual claim abstain). Real data: 5.2% of the 1434 the judged_pairs traces have no decidable commitment; your 5 GT clip traces ranged 0.20-0.80 decided_fraction. // It never actually NaN'd a step in any of the 69 logged steps across a1npli/eivn91/vtx3ys, but it was latent, and its sibling (the no-CoC missing-keys bug) did fire once in vtx3ys. 
    
  # Three bugs fixed: 
    # NaN poisons W&B step means - cosmos-rl's aggregate_report_Data uses plain np.mean; undecided rollouts now carry the group's decided-only mean instead of NaN. 
    # Missing aux keys count as 0 - the no-CoC branch emitted only one key, and the aggregator (?) 0-fills missing keys into means; every rollout nwo emits the full key set. 
    # Perverse abstain ordering - all-abstain scored -0.2 (gate PASS) while half-verified scored -0.5 (gate FAIL). Now coverage-blended: r_eff = decided_fraction + (1-df)*0.8, with the unparsed penalty aplied in the previously short-circuited undecided path. Sanity check on a real scene: all-abstain -0.263 < partially-verified GT -0.145; false "stop" claim still fails the gate at -0.600. 

    # all 100 tests passing (the scene_overlay_test.py collection error pre-exists my change - missing dep in this venv) plus behavioral checks on real clip data (what clips are we specifically referencing?)
    
   # BUGS.md 

   # code-reward W&B metrics silently biased/NaN-poisoned + all-abstain traces out-scored half-verified ones? what does this really mean?
   # what does it mean for something to be half-verified in thsi case?

   # Symptom: code_reward_raw/code_atomic_precision are written as math.nan when a rollout decides nothing, and the no-CoC branch emitted an aux dict with only code_scene_available.

   # Keys: metric names in that dictionary: