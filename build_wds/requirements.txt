# Lilypad pip overlay for WDS *build* jobs (build_wds_loop / build_webdataset.py).
# physical_ai_av requires Python >=3.11; cluster workers are 3.10. build_wds/configs/cluster.yaml
# sets PIP_IGNORE_REQUIRES_PYTHON=1 so pip installs anyway. Full resolution against
# lilypad-py on 3.10 is skipped in launch.py — only static checks run for this file.
#
# AV1 camera transcoding uses system ffmpeg (libsvtav1 or libaom-av1), not a pip package.

webdataset>=0.2.86
boto3>=1.34
huggingface_hub>=0.36.0
physical_ai_av
av>=14.4.0
pandas>=2.0
pyarrow
scipy>=1.15.0
tqdm
