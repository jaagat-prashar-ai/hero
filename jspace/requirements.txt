# Dedicated venv (jlens needs transformers>=5.5, which breaks the AR1 and
# alpamayo1_5 4.x-era envs — load_model.py shims the one incompatibility):
#   uv venv --python 3.12 jspace/.venv
#   uv pip install --python jspace/.venv/bin/python \
#     --extra-index-url https://download.pytorch.org/whl/cu128 -r requirements.txt
torch==2.8.0
torchvision==0.23.0
pillow
einops
hydra-core
scipy
accelerate
# J-lens reference implementation (Anthropic, Apache-2.0):
jlens @ git+https://github.com/anthropics/jacobian-lens
