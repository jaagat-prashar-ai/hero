# Lilypad pip overlay for masking *inference* (masking_loop on GPU workers).
# Validated by launch.py (uv pip compile against lilypad-py on Python 3.10).
#
# alpamayo1_5 model code is vendored at third_party/alpamayo1.5 (git submodule).
# torch MUST be pinned — base image ships 2.1.1; transformers>=4.57 needs >=2.4.
# See ~/knowledge/gotchas/clac-lilypad-torch-nccl.md and lead_sft_lilypad/requirements.txt.

torch==2.7.1
transformers==4.57.1
accelerate>=1.12.0
hydra-core>=1.3.2
huggingface_hub>=0.36.0
einops>=0.7
webdataset>=0.2.86
s3transfer>=0.10.0
boto3>=1.34
# pandas/av floors match third_party/alpamayo1.5/pyproject.toml's own minimums --
# that package isn't pip-installed here (sys.path-injected by bootstrap.py since
# it declares Python ==3.12.* and we run 3.10), so nothing else enforces them.
pandas>=2.3.3
pyarrow
scipy>=1.15.0
av>=16.0.1
