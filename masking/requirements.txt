# Dependencies for the masking experiment and WDS build pipeline.
# Installed by Lilypad before the workload runs (pip_requirements_path).

# Masking inference
# torch pin is REQUIRED by lilypad/launch.py validation: the base image ships
# torch 2.1.1, too old for transformers>=4.57, so the pip overlay must bring 2.7.1.
torch==2.7.1
# The cluster image's transformers is too old for alpamayo1_5 (needs
# Qwen3VLConfig, added in 4.57) — run masking-run9-d-dz71co died on exactly
# this import after the pin was lost in b79f73b; full set restored from f47a187.
transformers==4.57.1
accelerate>=1.12.0
hydra-core>=1.3.2
webdataset>=0.2.86
einops>=0.7
s3transfer>=0.10.0
boto3>=1.34
# alpamayo_r1 (model_family "a1") imports colorlog via alpamayo_r1.common.logging
colorlog>=6.0.0

# WDS build pipeline (build_webdataset.py) — requires Python >= 3.11
huggingface_hub>=0.36.0  # hf_hub_download, login
pandas>=2.3.3            # parquet loading for metadata / splits
pyarrow
scipy>=1.15.0
av>=16.0.1
