# Dependencies for the masking experiment and WDS build pipeline.
# Installed by Lilypad before the workload runs (pip_requirements_path).
# torch/transformers/einops are expected to be present in the cluster image.

# Masking inference
webdataset>=0.2.86
einops>=0.7

# WDS build pipeline (build_webdataset.py)
# physical_ai_av and its deps (PIP_IGNORE_REQUIRES_PYTHON=1 set in cluster YAML)
physical_ai_av           # NVIDIA PhysicalAI-AV devkit
av>=14.4.0               # PyAV video decode (required by physical_ai_av)
huggingface_hub>=0.36.0  # physical_ai_av requires >=0.36.0
pandas>=2.0
pyarrow                  # parquet support for physical_ai_av
scipy>=1.15.0            # stub RigidTransform in build_wds_worker.py covers the missing API
tqdm
numpy
