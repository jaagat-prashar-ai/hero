name: masking-cot-cluster
model_identifier: other.training.alpamayo15.default.v1
workload_variant_config:
  workload_type: "training"
  training_fn: masking.training.run.masking_loop
  training_fn_config:
    # ── S3 data source ────────────────────────────────────────────────────────
    # Replace PLACEHOLDER_BUCKET and PLACEHOLDER_PREFIX with your actual bucket
    # and prefix where the WDS shards were written by build_webdataset.py.
    # Example: s3://my-research-bucket/projects/masking/wds
    s3_bucket: "research-datasets-chicago"
    s3_prefix: "nvidia_physicalai_datasets/PhysicalAI-Autonomous-Vehicles/wds"
    local_data_dir: "/mnt/work/tmp/wds_cache"

    # Set to an integer to download only the first N shards (smoke test / debug).
    # Omit or set to null to download all shards.
    max_shards: null

    # 52-clip OOD sample manifest: clips are pulled from S3 via range reads at
    # iteration time, skipping shard downloads entirely. WITHOUT this, rank 0
    # mirrors the ENTIRE s3_prefix onto node-local /mnt/work — run
    # masking-run9-d-cr0skx (2026-07-27) killed its node that way (~26 min of
    # bulk shard downloads until the node died; ActorDiedError: "node has
    # died") before processing a single clip. Runs 8 a/b/c only worked because
    # this was passed as a -o override at launch.
    sample_clips_manifest: "masking/configs/sample_clips.json"

    # Durable S3 upload of the results JSONL after every write — outdir below
    # is node-local and dies with the pod (run8-A's results were lost that way).
    results_s3_prefix: "masking_results/run9"

    # ── Model ─────────────────────────────────────────────────────────────────
    checkpoint: "nvidia/Alpamayo-1.5-10B"

    # ── Experiment ────────────────────────────────────────────────────────────
    # "a" — masked vs unmasked trajectory ADE across all clips
    # "b" — per-word steering salience + concept-set ablation
    # "c" — prefix/suffix threshold sweep (ADE vs. CoT word position)
    experiment: "a"
    seed: 0

    # Comma-separated concept words for experiment B ablation
    concepts: "pedestrian,person,cyclist,crosswalk,vehicle,stop,red,light"

    # Word-count thresholds for experiment C prefix/suffix sweep
    threshold_words: [0, 5, 10, 20, 30, 50]

    # Resume from an interrupted run (skips already-written JSONL rows)
    resume: true

    # ── Distributed sharding (auto-set by Lilypad via RANK / WORLD_SIZE) ─────
    # With num_gpus: 8 each replica owns events where hash(clip_id,t0_us) % 8 == rank.
    # Outputs: batch_experiment_{experiment}_rank{RR}.jsonl (one file per GPU).

    # ── Output ────────────────────────────────────────────────────────────────
    outdir: "/mnt/work/tmp/masking_results"

  experiment_tracker:
    wandb:
      entity: research
      project: masking-cot

cluster_resources:
  gpu_machine_type: a100.8
  num_gpus: 8
  customer_resource: research
  preemptible: always
  allowed_regions:
    - us-chicago-1
  requeue_if_preempted: true

runtime_environment:
  # torch==2.7.1 pip install is ~3 GB per worker; allow ample setup time.
  setup_timeout_seconds: 3600

  required_environment_variables:
    - HF_TOKEN

  constant_environment_variables:
    AWS_REQUEST_CHECKSUM_CALCULATION: when_required
    AWS_RESPONSE_CHECKSUM_VALIDATION: when_required
    AWS_ENDPOINT_URL_S3: "https://idskhu5vqvtl.compat.objectstorage.us-chicago-1.oraclecloud.com"
    AWS_DEFAULT_REGION: us-chicago-1

  code_assets:
    root_directory: .
    pip_requirements_path: masking/requirements.txt
    regex_excludes:
      # .venv is 7.8G and made the code-assets zip 6.4GB — concurrent pod
      # setups crash-looped on it for an hour (run10, 2026-08-11, five
      # workloads STOPPED with zero progress). The env comes from
      # pip_requirements_path; the local venv must never ship.
      - "^/\\.venv/"
      - "^/build_wds/"
      - "^/mnist/"
      - "^/research-core/"
      - "^/docker/"
      - "^/lockfiles/"

